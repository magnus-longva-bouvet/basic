super secret value here. Should never ever be inside the Docker build engine's context, and I would rather die than rely on my Dockerfile's COPY commands to keep my secret value from ending up inside the image
